# Release Pilot configuration for multi-ecosystem project
# Rust crate + Node.js bindings

packages:
  - name: core
    ecosystem: cargo

  - name: node-bindings
    ecosystem: npm
    path: ./bindings/node

# Rust first, then Node bindings
releaseOrder:
  - core
  - node-bindings

version:
  devRelease: true
